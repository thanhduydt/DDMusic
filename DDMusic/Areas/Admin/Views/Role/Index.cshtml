@model DDMusic.Areas.Admin.Controllers.RoleController
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Users</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a asp-action="Users">Users</a></li>
                        <li class="breadcrumb-item active"></li>
                    </ol>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>
    <!-- Main content -->
    <section class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card">

                        <!-- /.card-header -->
                        <div class="card-body">
                            <a asp-action="CreateRole" asp-controller="Role"><i title="Thêm" class="nav-icon fas fa-plus"></i></a>
                            <table id="example1" class="table table-bordered table-striped">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Tên</th>
                                        <th>Thao tác</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (ViewBag.listRole != null)
                                    {
                                        foreach (var role in ViewBag.listRole)
                                        {
                                            <tr>

                                                <td>@role.Id</td>
                                                <td>@role.Name</td>

                                                <td>
                                                    <a asp-controller="Role" asp-action="EditRole" asp-route-id="@role.Id"><i title="Cập nhật" class="nav-icon fas fa-edit"></i></a>
                                                    <a asp-controller="Role" asp-action="DeleteRole" asp-route-id="@role.Id" onclick="return confirm('Bạn có muốn xóa không?');"><i title="Xóa" class="fas fa-trash-alt"></i></a>
                                                </td>
                                            </tr>
                                        }
                                    }
                                </tbody>

                            </table>
                        </div>
                        <!-- /.card-body -->
                    </div>
                    <!-- /.card -->
                </div>
                <!-- /.col -->
            </div>
            <!-- /.row -->
        </div>
        <!-- /.container-fluid -->
    </section>
    <!-- /.content -->
</div>